# BASE IMAGE TO USE (NGINX)
FROM nginx:latest

# MAINTAINER OF THIS DOCKERFILE
USER jessicacastro

# INITIAL WORKING DIRECTORY ON CONTAINER
WORKDIR /app

# INSTALL VIM ON CONTAINER
RUN apt-get update && \
  apt-get install vim -y

# COPY HTML FILES FROM HTML FOLDER ON THIS REPOSITORY TO NGINX HTML DIRECTORY
# THE INDEX HTML FILE WILL BE SERVED BY NGINX INSTEAD OF THE DEFAULT INDEX.HTML FROM ITS BASE IMAGE
# Copy from sibling directory (html/)
COPY html/ /usr/share/nginx/html
